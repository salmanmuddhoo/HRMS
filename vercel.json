{
  "version": 2,
  "installCommand": "npm install && cd backend && npm install && npx prisma generate && cd ../frontend && npm install --legacy-peer-deps",
  "buildCommand": "cd backend && npx prisma db push --skip-generate && cd ../frontend && CI=false npm run build",
  "outputDirectory": "frontend/build",
  "functions": {
    "api/index.ts": {
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
